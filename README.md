
  # MONO BOLD Portfolio Template

  This is a code bundle for MONO BOLD Portfolio Template. The original project is available at https://www.figma.com/design/cDPe1ePqBbn1mNvmDHbmmI/MONO-BOLD-Portfolio-Template.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  